Requirements from bookshelf-pwa-requirements.md: capture client around getUserMedia + canvas snapshot fallback, ImageCapture.takePhoto() as best-effort enhancement. Generic Sensor API for IMU data (Accelerometer, Gyroscope, LinearAccelerationSensor, AbsoluteOrientationSensor, GravitySensor, Magnetometer). PWA: HTTPS + service worker + Workbox BackgroundSyncPlugin + IndexedDB blob queue + navigator.storage.persist() + Screen Wake Lock + standalone manifest. Quality feedback on 320x240 downscaled grabFrame at 5-10Hz using plain Canvas/WebGL (no OpenCV.js). Multi-device: Scan (library-wide) > CaptureSession (per device) > CaptureRecord (per photo). Server-anchored clockOffsetMs for cross-device time sync. Continuous IMU trace at 50-60Hz (~9MB/30min). Ghost overlay via CSS transform translate3d driven by gyro integration. Stack: Vite + TypeScript + React, vite-plugin-pwa + Workbox, idb, exifr, qrcode-svg. @types/w3c-image-capture + hand-written sensors.d.ts.
